Angela M. Hipolito

1. the difference between git clone and git pull is the git clone duplicates the git, while the git pull pull a request to the git.
2. git checkout -b feature-login
3. git status shows the branch that you are currently in, and it displays the repositorys status.
4. pull request is used to merge and combine files in a repository, it is useful in teams because it can combine different files without confusion.
5. the purpose of git add and git commit is to add a file in the repository.

section 2.

5. true 
   false

