PART A: 
SECTION 1
1. The difference between git clone and git pull, is that git clone is, like cloning the url, so that it can be connected to the command prompt. While the git pull is like pulling a request.

2. The command to create a new branch is "git checkout -b feature-login".

3. The git status show if your prompt is working. It also shows where branch you are in.

4. A Pull Request(PR) is asking the other team member to accept the request to make two or three activities to merge.

5. The purpose of git add and git commit, is that git add is adding your folder name of file name so that it can be connected. While the git commit is the message.

SECTION 2:
5. true
   false