Rainrev Tomimbang Carbonell

1. What is the difference between git clone and git pull? 

the difference between git clone and git pull is git clone works like copying a file to another device or place but instead of simply copying it, it clones the repository inside your computer remotely using github, while git pull retrieves or fetches the code remotely from your repository

2. Write the command to create a new branch named feature-login.
git checkout -b feature-login

3. What does git status show?
checks if you're in the right branch and checks for whether you uploaded a file or no

4. Explain what a Pull Request (PR) is and why teams use it.
This is where you compare and merge the newly made code. This is also where you check if your team has an overlapping code 

5. Explain the purpose of git add and git commit.
git add -  git add functions like a landing pad it adds the newly made file or code in the current branch but it still waits for a launch code which is the git commit. While git commit is like the confirmation or the deciding factor on which decides if your code gets uploaded or not Without these two your code won't even reach the repository.

SECTION 2

true 
false